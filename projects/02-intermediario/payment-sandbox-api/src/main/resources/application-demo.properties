# Select with -Dspring-boot.run.profiles=demo inside the Dev Container.
# No fallback to the development database or its credentials.
spring.datasource.url=jdbc:postgresql://postgres:5432/payments_demo
spring.datasource.username=payments_demo
spring.datasource.password=${DEMO_DB_PASSWORD}
